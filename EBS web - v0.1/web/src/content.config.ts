/**
 * EBS Content Collections
 *
 * Currently unused — all programme data lives in src/data/programmes.ts
 * and page content is inline in .astro files.
 *
 * This file is kept as a placeholder. Add collections here when
 * migrating to content-driven pages (e.g., blog, news, events).
 */

export const collections = {};
